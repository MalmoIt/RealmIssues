<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="BokaRealmPoc.Views.AddLandPage"
             Title="Add land">
  <ContentPage.ToolbarItems>
    <ToolbarItem Text="One" 
                 Priority="1"
                 Command="{Binding AddLandCommand}"/>   
    <ToolbarItem Text="100 land" 
                 Priority="1"
                 Command="{Binding AddManyLandsCommand}"/>
  </ContentPage.ToolbarItems>
  <ListView ItemsSource="{Binding Lands}"
            VerticalOptions="FillAndExpand"
            ItemSelected="OnLandSelected"
            HasUnevenRows="True">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout Orientation="Horizontal"
                       Spacing="20">
            <Label Text="{Binding Title}"
                   HorizontalOptions="FillAndExpand"/>

            <Label Text="X" 
                   HorizontalOptions="End"
                   IsVisible="{Binding IsSelected}"/>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>